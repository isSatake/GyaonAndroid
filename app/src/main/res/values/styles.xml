<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="GyaonButtonStyle" parent="android:Widget.Button">
        <item name="android:background">@drawable/recbutton_background</item>
        <item name="android:textColor">#FFF</item>
    </style>
</resources>
